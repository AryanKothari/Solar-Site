import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.licdn.com", "m.media-amazon.com", "images.thdstatic.com"], // Add the hostname here
  },
  // other config options here
};

export default nextConfig;
